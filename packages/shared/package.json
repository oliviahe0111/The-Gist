{
  "name": "@the-gist/shared",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "dev": "tsc --watch",
    "lint": "eslint src/**/*.{ts,tsx}"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
